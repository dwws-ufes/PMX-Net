package br.ifes.pmxnet.dominio.dto;

/**
 * Projeção baseada em interface para gerar relatórios de interações
 * agrupadas por ferramenta e tipo.
 */
public interface RelatorioInteracaoDTO {
    String getFerramenta();
    String getTipo();
    Long getQuantidade();
}
